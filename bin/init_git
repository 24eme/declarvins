#!/bin/bash

cat .gitmodules | sed 's/https:\/\/git.gitorious.org\//git@gitorious.org:/' > /tmp/gitmodules && mv /tmp/gitmodules .gitmodules
cat .git/config | sed 's/https:\/\/git.gitorious.org\//git@gitorious.org:/' > /tmp/gitconfig  && mv /tmp/gitconfig .git/config
