#!/bin/bash

cat .gitmodules | sed 's/git@gitorious.org:/https:\/\/git.gitorious.org\//' > /tmp/gitmodules && mv /tmp/gitmodules .gitmodules
cat .git/config | sed 's/git@gitorious.org:/https:\/\/git.gitorious.org\//' > /tmp/gitconfig  && mv /tmp/gitconfig .git/config
